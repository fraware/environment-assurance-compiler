{
  "schema_version": 1,
  "id": "E1",
  "slug": "refund",
  "title": "Support refund workflow",
  "description": "OpenAPI + SOP + trace conflicts fail closed; E1 compiles only the non-conflicted path into an EF-0 executable authoring pack.",
  "status": "contract-complete",
  "requires": {
    "python": ">=3.11",
    "envassure": "*",
    "extras": []
  },
  "entrypoints": {
    "run": "run.sh",
    "verify": "verify.sh"
  },
  "paths": {
    "input": "input",
    "expected": "expected"
  },
  "depends_on_integrations": [],
  "notes": [
    "Invoke run.sh and verify.sh through bash with eac from an installed wheel.",
    "Reconciliation must fail with EAC4027; timeout/retry/idempotency conflicts remain open and are excluded from executable IR rather than silently decided.",
    "The scoped model remains EF-0 with live_payment_rail and timeout_retry_idempotency_semantics unsupported; verify-pack proves structural integrity, not high fidelity."
  ]
}
