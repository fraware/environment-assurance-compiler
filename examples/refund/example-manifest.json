{
  "schema_version": 1,
  "id": "E1",
  "slug": "refund",
  "title": "Support refund workflow",
  "description": "OpenAPI + SOP + trace evidence is reconciled fail-closed, explicitly decided, then compiled into an EF-0 executable authoring pack.",
  "status": "contract-complete",
  "requires": {
    "python": ">=3.11",
    "envassure": "*",
    "extras": []
  },
  "entrypoints": {
    "run": "run.sh",
    "verify": "verify.sh"
  },
  "paths": {
    "input": "input",
    "expected": "expected"
  },
  "depends_on_integrations": [],
  "notes": [
    "Invoke run.sh and verify.sh through bash with eac from an installed wheel.",
    "Pre-decision reconciliation must fail with EAC4027; the example records an explicit example-author decision before build/lint/run.",
    "The decided model remains EF-0 with live_payment_rail unsupported; verify-pack proves structural integrity, not high fidelity."
  ]
}
